{
  "scripts": {
    "cypress:open": "cypress open",
    "start-server": "python application.py",
    "test": "$(npm bin)/cypress run",
    "ci": "start-server-and-test install start-server http://localhost:5000 test"
  },
  "name": "cypress",
  "version": "1.0.0",
  "main": "index.js",
  "dependencies": {
    "cypress": "^2.1.0"
  },
  "devDependencies": {
    "start-server-and-test": "^1.4.1"
  },
  "author": "",
  "license": "ISC",
  "description": ""
}
